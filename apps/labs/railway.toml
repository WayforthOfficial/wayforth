[build]
builder = "dockerfile"
dockerfilePath = "apps/labs/Dockerfile"

[deploy]
startCommand = "uv run uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
